//fname = JOptionPane.showInputDialog(null, "nom du fichier?");

String fpath = buffer.getPath();

if (fpath != "") {
  File f = new File(fpath);
  long lc = f.lastModified();
  f.setLastModified(lc - 1000 * 60 * 60);
}

