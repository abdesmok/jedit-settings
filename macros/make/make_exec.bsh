source(jEdit.getSettingsDirectory() + "/macros/make/command.bsh");

File jeditMakefile = new File(app + "/Makefile.jedit");

//JOptionPane.showMessageDialog(null, app + " " + jeditMakefile.getPath() + " " + jeditMakefile.exists());

if (jeditMakefile.exists()) {
  command("make exec -f Makefile.jedit");
}
else {
  command("make exec");
}

