textArea.goToPrevWord(true,false);
textArea.goToPrevWord(true,false);
Registers.copy(textArea,'$');
textArea.goToNextCharacter(false);
textArea.setSelectedText("x(), ");
Registers.paste(textArea,'$',false);
textArea.setSelectedText("y(), ");
Registers.paste(textArea,'$',false);
textArea.setSelectedText("width(), ");
Registers.paste(textArea,'$',false);
textArea.setSelectedText("height()");

